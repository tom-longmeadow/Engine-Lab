[package]
name = "impls"
version = "0.1.0"
edition = "2024"

[dependencies]
base = { path = "../base" }
derive_more = { version = "1.0.0", features = ["full"] }

[dev-dependencies]
base = { path = "../base", features = ["testing"] }

